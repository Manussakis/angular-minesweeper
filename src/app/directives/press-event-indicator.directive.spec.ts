import { PressEventIndicatorDirective } from './press-event-indicator.directive';

describe('PressEventIndicatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PressEventIndicatorDirective();
    expect(directive).toBeTruthy();
  });
});
